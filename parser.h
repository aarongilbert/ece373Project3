#ifndef PARSER_H
#define PARSER_H

extern char *traceEntry;
extern struct inst instructions[250];

void stringFromFile(char* fileName);
void generateInstructions()
int getInstNumber(char* inst)

#endif